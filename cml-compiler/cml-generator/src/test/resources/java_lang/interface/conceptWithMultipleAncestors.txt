interface Book extends Product, StockItem
{
    String getTitle();

    static Book create(Product product, StockItem stockItem, String title)
    {
        return new BookImpl(product, stockItem, title);
    }
}